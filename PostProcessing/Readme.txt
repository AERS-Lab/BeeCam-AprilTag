*************************************************************************************
The two folders contain two different versions of the script called "Analysis". 
The one inside the folder "Colony_Processing" executes an analysis for one single data file.
For the folder "Apiary_Procesing" the anaylisis is perform over 6 data files, generating one single general report file.
Both call the same functions inside the folder "Functions".
